【粘贴 00_master.md 内容】

只修改 【包名】/exception.py，不改其他文件。

问题：validate_remote_path 对合法路径也返回错误。

检查并修复以下两点：
1. 常量 【路径前缀常量名】 的值末尾必须有斜杠，必须等于 "【路径前缀】"
2. startswith() 里必须用 【路径前缀常量名】 变量，不能用字符串字面量

不允许改变函数名和返回值结构。
只输出修复后的完整 exception.py。
