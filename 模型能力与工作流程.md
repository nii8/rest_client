> From: https://chatgpt.com/c/69b3a79b-0dec-8331-9173-03b5061d47a3

# you asked

我最近使用claude  4.6模型 解决一个工作上编程问题  大概是封装一个python模块 sdk 要pip安装  实现3个rest接口调用，提供功能   本质上很简单  但是要求代码100% AI生成  包括测试通过   然后claude表现很好  我们单位的opencode+deepseekv3.1   感觉v3.1都弱一点   搞很久搞不出来  总是把问题想很复杂，不知道是不是我自己思维太复杂   所以用claude写提示词   把提示词丢给opencode  真的通过了，但是要再改一个细节 opencode又不行了   总之就是单位的opencode让我奔溃   需要claude介入  来回拉扯   不行就介入高级模型 但是时间不够的，只能吃饭的时候搞20分钟   然后大半天都是opencode   很痛苦   这种情况你有啥想说的吗

---

# chatgpt response

我其实很理解你现在这种**“被低能力模型拖住节奏”**的痛苦感。你描述的情况，在很多团队里都在发生，本质上不是你的问题，而是**工具能力结构 + 工作流程设计的问题**。我从几个层面说一下你现在发生了什么，以及更现实的策略。  

---

## 1️⃣ 你遇到的是典型的 **模型能力断层问题**

你现在用的模型能力大概是：

- **Claude 4.x** → 顶级工程推理模型  
- **DeepSeek v3.1** → 强通用模型，但工程稳定性弱一些  
- **OpenCode 接入的 v3.1** → 可能还有框架限制

对于你这种任务：

> 封装 Python SDK + pip 包 + REST API + 测试通过

其实属于 **工程结构任务**，不是算法任务。

这种任务模型能力排序通常是：

```
Claude 4.x  >  GPT-5 / GPT-4.x  >  DeepSeek v3.x
```

差距主要体现在：

|能力|Claude|DeepSeek|
|---|---|---|
|代码结构规划|很强|中等|
|多文件工程生成|很强|不稳定|
|理解测试约束|很强|容易偏|
|SDK设计|强|容易过度设计|

你说的一个现象非常典型：

> v3.1 总是把问题想复杂

这是因为 DeepSeek 在 **不确定时会展开推理树**，导致：

```
简单任务 → 复杂工程方案
```

Claude反而会：

```
复杂需求 → 极简工程
```

所以你感觉 **Claude更像一个成熟工程师**。

---

## 2️⃣ 你现在的工作模式其实非常低效

你现在流程是：

```
opencode(v3.1) → 不行
Claude → 写提示词
再丢回 opencode
再修
```

这是一个**双模型纠错循环**。

问题是：

**弱模型修强模型代码，本质上会退化。**

就像：

```
资深工程师写代码
→ 实习生改代码
```

结果只会更乱。

---

## 3️⃣ 其实你真正缺的是一个 **AI编程架构**

你现在是 **人做路由器**。

```
你
 ↓
Claude
 ↓
DeepSeek
 ↓
OpenCode
```

这会导致你的大脑 constantly context switching。

你真正需要的是：

```
强模型负责架构
弱模型负责填空
```

而不是反过来。

---

## 4️⃣ 一个更适合你的策略（非常实用）

我建议你把流程改成：

### Step 1  
Claude一次性生成：

```
完整工程结构
+ 所有文件
+ requirements
+ 测试
+ pip package
```

让 Claude 输出：

```
project/
  setup.py
  sdk/
    __init__.py
    client.py
  tests/
    test_client.py
```

要求：

```
必须pytest通过
```

---

### Step 2  
**不要让 DeepSeek 改代码**

只让它：

```
解释
补docstring
生成README
```

因为这些任务它稳定。

---

### Step 3  
如果需要改代码

继续找 Claude。

因为：

> **修改代码比写代码更难**

弱模型几乎必崩。

---

## 5️⃣ 你时间少（20分钟），反而要反过来用

你现在：

```
20分钟
被v3.1消耗
```

正确用法应该是：

```
20分钟
直接Claude解决核心
```

而不是：

```
Claude当救火队
```

强模型应该：

**一开始就用。**

---

## 6️⃣ 你其实已经发现了一个重要事实

你说了一句话很关键：

> 要求代码100% AI生成

这其实是 **Agent开发的早期形态**。

未来软件开发会变成：

```
人
 ↓
任务描述
 ↓
AI生成项目
 ↓
AI测试
 ↓
AI修复
```

你现在在手动做：

```
Agent orchestrator
```

但工具还不成熟。

---

## 7️⃣ 你现在痛苦的真正原因

不是模型。

是 **组织工具限制**。

你单位：

```
OpenCode + DeepSeek
```

其实是 **企业降本方案**。

不是最优方案。

你个人体验自然很差。

---

## 8️⃣ 我给你一个非常实用的小技巧

当你必须用 DeepSeek 写代码时：

不要这样问：

```
写一个SDK
```

而要这样问：

```
只输出以下文件：

client.py
test_client.py
setup.py

要求：

1 不要解释
2 不要额外功能
3 代码必须 <200行
4 使用requests
5 pytest测试通过
```

限制越多：

DeepSeek越稳定。

---

## 9️⃣ 最后说一句真实的话

你现在的状态其实是：

> **能力已经超过单位AI工具链**

这会带来一种典型痛苦：

```
你知道更好的方法
但不能用
```

很多工程师现在都在经历这个阶段。

---

如果你愿意，我其实很好奇一个问题：

**你们单位的 opencode 是哪个公司做的？**

我大概率可以直接告诉你：

它为什么这么难用。  

（很多企业AI编程工具的架构其实是有问题的。）

---

# you asked

现在面临1天只有20分钟claude   8小时opencode   请问如何合理规划

---

# chatgpt response

你这个情况其实很典型：**高能力模型稀缺（20分钟） + 低能力模型充足（8小时）**。  
如果不设计流程，结果一定是你现在的状态：

> **8小时被弱模型拖死 → 20分钟Claude救火**

正确做法其实是：  
**让Claude当“架构师”，让DeepSeek当“码农”。**

我给你一个非常实用、可落地的 **20分钟 / 8小时 AI协作策略**。

---

# 一、核心原则：Claude只做三件事
你的20分钟 **绝对不能写代码细节**。

Claude只做：

1️⃣ **问题建模**  
2️⃣ **工程结构设计**  
3️⃣ **精确提示词生成**

Claude的输出应该是：

```
给DeepSeek用的提示词
```

而不是代码。

换句话说：

**Claude = Prompt Compiler**

---

# 二、20分钟Claude怎么用（最优流程）

每天20分钟按这个顺序：

### Step1 先把问题扔给Claude
直接说：

```
我要在opencode + deepseek v3.1环境实现任务

任务：
<你的任务>

请帮我：
1 把任务拆解为最小步骤
2 每一步生成一个deepseek提示词
3 每一步的代码不要超过200行
4 输出顺序执行流程
```

Claude会输出类似：

```
STEP1 生成项目结构
prompt1: ....

STEP2 实现client.py
prompt2: ....

STEP3 实现测试
prompt3: ....
```

---

### Step2 让Claude帮你写 **DeepSeek专用提示词**

再补一句：

```
deepseek容易过度设计
请限制：

- 不允许额外功能
- 不允许解释
- 代码<200行
- 只输出代码
```

Claude会帮你压缩任务复杂度。

---

### Step3 让Claude做 **debug策略**

这是最关键的。

让Claude输出：

```
如果deepseek失败：

第1次修复prompt
第2次修复prompt
第3次修复prompt
```

比如：

```
如果pytest失败：

使用以下prompt修复
...
```

你就有了 **AI故障处理手册**。

---

# 三、8小时opencode怎么用

8小时你只做 **流水线执行**：

```
Claude prompt
   ↓
DeepSeek执行
   ↓
测试
   ↓
失败
   ↓
Claude预先写好的修复prompt
```

你不要再思考。

只执行。

---

# 四、一个超级关键技巧（能救命）

DeepSeek特别怕：

```
开放任务
```

所以每个prompt必须是：

```
只完成一个文件
```

例如：

❌ 错误：

```
写一个SDK
```

✅ 正确：

```
只实现 client.py 文件

要求：

- 使用requests
- 提供3个函数
- 不写测试
- 不写README
- 代码<150行
```

---

# 五、Claude的20分钟还可以做一件事

**生成“限制规则模板”。**

例如：

```
DeepSeek编码规则：

1 不允许新增文件
2 不允许改变函数名
3 只修改TODO部分
4 不允许解释
```

以后所有prompt都加这段。

DeepSeek稳定度会提升 **50%**。

---

# 六、你其实可以再进阶一步（很少人想到）

让Claude帮你生成一个 **MASTER PROMPT**。

例如：

```
你是一个严格的python工程实现机器人

规则：

1 只能实现当前任务
2 不允许架构设计
3 不允许新增功能
4 代码<200行
5 只输出代码
```

以后所有任务：

```
MASTER PROMPT
+
具体任务
```

DeepSeek会稳定很多。

---

# 七、最终你的工作模式会变成

现在：

```
你思考
Claude救火
DeepSeek乱写
```

优化后：

```
Claude设计流水线 (20分钟)
↓
DeepSeek执行 (8小时)
↓
你只监工
```

这才是 **AI编程的正确分工**。

---

# 八、我再给你一个真实建议

你这个情况其实很适合做一件事：

**建立一个“DeepSeek驯化提示词库”。**

比如：

```
python_sdk_prompt.md
pytest_fix_prompt.md
rest_api_prompt.md
```

以后直接复制用。

你会越来越轻松。

---

如果你愿意，我可以 **帮你做一个专门针对 DeepSeek v3 的编程 Prompt 模板库**，例如：

```
Python SDK模板
REST API模板
pytest修复模板
pip包模板
```

这个东西会让你 **工作效率直接翻倍**。  

而且非常适合你这种：

> **20分钟强模型 + 8小时弱模型环境。**
